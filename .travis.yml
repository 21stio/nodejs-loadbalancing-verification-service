sudo: required
language: generic

services:
- docker

script:
  - docker build --tag nodejs-loadbalancing-verification-service --tag nodejs-loadbalancing-verification-service:latest --tag nodejs-loadbalancing-verification-service:$(date "+%d-%m-%Y_%H-%M-%S") .
  - docker login --username ${REGISTRY_USERNAME} --password ${REGISTRY_PASSWORD}
  - docker push nodejs-loadbalancing-verification-service